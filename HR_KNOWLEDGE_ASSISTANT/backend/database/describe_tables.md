# Table: CHAT_HISTORY

## DDL:

  CREATE TABLE "CHAT_HISTORY" 
   (	"CHAT_ID" NUMBER GENERATED BY DEFAULT AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
	"USERNAME" VARCHAR2(100) NOT NULL ENABLE, 
	"QUESTION" CLOB NOT NULL ENABLE, 
	"ANSWER" CLOB, 
	"SOURCE_DOCUMENT" VARCHAR2(255), 
	"CREATED_AT" TIMESTAMP (6) DEFAULT CURRENT_TIMESTAMP, 
	 PRIMARY KEY ("CHAT_ID")
  USING INDEX  ENABLE
   ) ;

## Columns:
  - CHAT_ID - NUMBER NOT NULL [pk]
  - USERNAME - VARCHAR2(100) NOT NULL
  - QUESTION - CLOB NOT NULL
  - ANSWER - CLOB
  - SOURCE_DOCUMENT - VARCHAR2(255)
  - CREATED_AT - TIMESTAMP

## Constraints:
  - CONSTRAINT - PRIMARY KEY (CHAT_ID)

## Indexes:
  - SYS_C0013992 - UNIQUE (CHAT_ID)

# Table: CHAT_MESSAGES

## DDL:
  CREATE TABLE "CHAT_MESSAGES" 
   (	"MESSAGE_ID" NUMBER GENERATED ALWAYS AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
	"SESSION_ID" VARCHAR2(100) NOT NULL ENABLE, 
	"ROLE" VARCHAR2(20) NOT NULL ENABLE, 
	"CONTENT" CLOB NOT NULL ENABLE, 
	"CREATED_AT" TIMESTAMP (6) DEFAULT CURRENT_TIMESTAMP, 
	 PRIMARY KEY ("MESSAGE_ID")
  USING INDEX  ENABLE
   ) ;

  ALTER TABLE "CHAT_MESSAGES" ADD CONSTRAINT "FK_CHAT_MESSAGES_SESSION" FOREIGN KEY ("SESSION_ID")
	  REFERENCES "CHAT_SESSIONS" ("SESSION_ID") ON DELETE CASCADE ENABLE;

## Columns:
  - MESSAGE_ID - NUMBER NOT NULL [pk]
  - SESSION_ID - VARCHAR2(100) NOT NULL [fk]
  - ROLE - VARCHAR2(20) NOT NULL
  - CONTENT - CLOB NOT NULL
  - CREATED_AT - TIMESTAMP

## Constraints:
  - CONSTRAINT - PRIMARY KEY (MESSAGE_ID)
  - FK_CHAT_MESSAGES_SESSION - FOREIGN KEY (SESSION_ID) -> CHAT_SESSIONS(SESSION_ID)

## Indexes:
  - SYS_C0014019 - UNIQUE (MESSAGE_ID)

# Table: CHAT_SESSIONS

## DDL:
  CREATE TABLE "CHAT_SESSIONS" 
   (	"SESSION_ID" VARCHAR2(100), 
	"TITLE" VARCHAR2(200), 
	"CREATED_AT" TIMESTAMP (6) DEFAULT CURRENT_TIMESTAMP, 
	 PRIMARY KEY ("SESSION_ID")
  USING INDEX  ENABLE
   ) ;

## Columns:
  - SESSION_ID - VARCHAR2(100) NOT NULL [pk]
  - TITLE - VARCHAR2(200)
  - CREATED_AT - TIMESTAMP

## Constraints:
  - CONSTRAINT - PRIMARY KEY (SESSION_ID)

## Indexes:
  - SYS_C0014014 - UNIQUE (SESSION_ID)

# Table: DOCUMENT_CHUNKS

## DDL:
  CREATE TABLE "DOCUMENT_CHUNKS" 
   (	"CHUNK_ID" NUMBER GENERATED BY DEFAULT AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
	"DOCUMENT_ID" NUMBER NOT NULL ENABLE, 
	"CHUNK_NUMBER" NUMBER NOT NULL ENABLE, 
	"PAGE_NUMBER" NUMBER, 
	"SECTION_TITLE" VARCHAR2(255), 
	"CHUNK_TEXT" CLOB NOT NULL ENABLE, 
	"CREATED_AT" TIMESTAMP (6) DEFAULT CURRENT_TIMESTAMP, 
	"EMBEDDING" VECTOR(384, FLOAT32), 
	 PRIMARY KEY ("CHUNK_ID")
  USING INDEX  ENABLE
   ) ;

  ALTER TABLE "DOCUMENT_CHUNKS" ADD CONSTRAINT "FK_DOCUMENT_CHUNKS" FOREIGN KEY ("DOCUMENT_ID")
	  REFERENCES "HR_DOCUMENTS" ("DOCUMENT_ID") ON DELETE CASCADE ENABLE;

## Columns:
  - CHUNK_ID - NUMBER NOT NULL [pk]
  - DOCUMENT_ID - NUMBER NOT NULL [fk]
  - CHUNK_NUMBER - NUMBER NOT NULL
  - PAGE_NUMBER - NUMBER
  - SECTION_TITLE - VARCHAR2(255)
  - CHUNK_TEXT - CLOB NOT NULL
  - CREATED_AT - TIMESTAMP
  - EMBEDDING - VECTOR

## Constraints:
  - CONSTRAINT - PRIMARY KEY (CHUNK_ID)
  - FK_DOCUMENT_CHUNKS - FOREIGN KEY (DOCUMENT_ID) -> HR_DOCUMENTS(DOCUMENT_ID)

## Indexes:
  - SYS_C0013987 - UNIQUE (CHUNK_ID)
  - CHUNK_TEXT_IDX - NONUNIQUE (CHUNK_TEXT)

# Table: HR_DOCUMENTS

## DDL:
  CREATE TABLE "HR_DOCUMENTS" 
   (	"DOCUMENT_ID" NUMBER GENERATED BY DEFAULT AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
	"TITLE" VARCHAR2(255) NOT NULL ENABLE, 
	"CATEGORY" VARCHAR2(100), 
	"FILE_NAME" VARCHAR2(255) NOT NULL ENABLE, 
	"UPLOAD_DATE" TIMESTAMP (6) DEFAULT CURRENT_TIMESTAMP, 
	"TOTAL_PAGES" NUMBER, 
	"STATUS" VARCHAR2(50) DEFAULT 'UPLOADED', 
	"FILE_PATH" VARCHAR2(500), 
	"FILE_HASH" VARCHAR2(64), 
	"UPLOADED_BY" VARCHAR2(100), 
	 PRIMARY KEY ("DOCUMENT_ID")
  USING INDEX  ENABLE
   ) ;
  
## Columns:
  - DOCUMENT_ID - NUMBER NOT NULL [pk]
  - TITLE - VARCHAR2(255) NOT NULL
  - CATEGORY - VARCHAR2(100)
  - FILE_NAME - VARCHAR2(255) NOT NULL
  - UPLOAD_DATE - TIMESTAMP
  - TOTAL_PAGES - NUMBER
  - STATUS - VARCHAR2(50)
  - FILE_PATH - VARCHAR2(500)
  - FILE_HASH - VARCHAR2(64)
  - UPLOADED_BY - VARCHAR2(100)

## Constraints:
  - CONSTRAINT - PRIMARY KEY (DOCUMENT_ID)

## Indexes:
  - SYS_C0013982 - UNIQUE (DOCUMENT_ID)
